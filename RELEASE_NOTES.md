# 춘구마 버튼 도우미 for macOS 1.1.0

카카오톡 미니게임 창에 포커스가 없어도 `밥 주기`·`물 주기` 버튼의 내부 누르기 액션을 직접 실행하는 기능을 추가했습니다.

## 포함된 기능

- 컵과 숟가락·포크 자동 인식
- 색 및 형태 반응을 확인한 뒤에만 다음 키 입력
- 입력 전 지연 0–500ms 설정, 기본값 120ms
- 접근성 AXPress를 이용한 버튼 직접 입력(기본값)
- 버튼 요소 미노출 시 카카오톡 프로세스 직접 키 입력으로 자동 전환
- 게임 보조 창의 두 버튼을 미리 찾아 첫 입력 탐색 지연 제거
- 전체 화면 대신 춘구마 게임 보조 창 하나만 캡처(오디오 비활성화)
- 결과 화면의 `게임 다시하기` 버튼 자동 실행 및 켜기·끄기 설정
- 현재 활성 앱 입력 호환 모드
- F8 전역 시작·정지 단축키
- Apple Silicon 및 Intel을 지원하는 Universal 앱
- 네트워크 통신 및 화면 저장 없음

## 설치

`ChoongumaButtonHelper-macOS-universal.zip`을 내려받아 압축을 풀고 `춘구마 버튼 도우미.app`을 실행하세요.

개인 개발용 ad-hoc 서명으로 배포되므로, 처음 실행이 차단되면 Finder에서 앱을 Control-클릭하고 **열기**를 선택하세요. 이후 화면 기록과 손쉬운 사용 권한을 허용해야 합니다.

자동 입력을 시작할 때 미니게임 보조 창을 열어 두어야 합니다. 창 전용 스트림이 시작된 뒤에는 다른 앱이 앞에 있거나 창을 최소화해도 현재 환경에서는 동작합니다. 전체 화면이 아니라 해당 게임 창 하나만 캡처하며 오디오는 캡처하지 않습니다.
